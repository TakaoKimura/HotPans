<% if @bread_store.nil? %>
  <h2>?????????????????</h2>
  <%= link_to "?????????", new_bread_store_path %>
<% elsif @breads.empty? %>
   <h2>????????????????</h2>
   <%= link_to "????????", new_bread_path %>
<% else %>

<h1>?????</h1>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr class="success">
      <th>???</th>
      <th>??</th>
      <th>??</th>
      <th>??</th>
      <th>??</th>
    </tr>
  </thead>

  <tbody>
    <% @breads.each do |bread| %>
      <tr class="active">
        <td><%= bread.name %></td>
        <td><%= bread.kind %></td>
        <td><%= link_to '??', bread %></td>
        <td><%= link_to '??', edit_bread_path(bread) %></td>
        <td><%= link_to '??', bread, method: :delete, data: { confirm: '?????????????' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '??????????', new_bread_path %>
<% end %>

