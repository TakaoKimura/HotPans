<%= form_for :bread, url: getFormActionURL(current_view), method: getFormMethod(current_view), html: { class: "float-left" } do |f|%>
  <%= f.hidden_field :name %>
  <%= f.hidden_field :kind %>
  <%= f.submit "確定", class: "btn btn-primary" %>
<% end %>
