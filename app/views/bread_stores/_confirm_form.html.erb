<%= form_for :bread_store, url: getFormActionURL(current_view), method: getFormMethod(current_view), html: { class: "float-left" } do |f| %>
	<%= f.hidden_field :name %>
	<%= f.hidden_field :phone_number %>
	<%= f.hidden_field :address %>
	<%= f.submit "確定", class: "btn btn-primary"%>
<% end %>
